import { createEmbeddedControls } from '../embedded/EmbeddedControls';

export default createEmbeddedControls({
  namespace: 'rooms.animeLiquidOrb',
  standalonePath: 'exhibits/anime-liquid-orb/index.html',
});
